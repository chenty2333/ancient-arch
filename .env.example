# Database Configuration
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=ancient_arch

# Application Configuration
# Note: In docker-compose, the host is 'db', not 'localhost'
# But for local cargo run, use 'localhost'
# DATABASE_URL is constructed automatically in docker-compose, 
# but you can override it here if needed.

JWT_SECRET=change_me_to_a_secure_secret
RUST_LOG=info
